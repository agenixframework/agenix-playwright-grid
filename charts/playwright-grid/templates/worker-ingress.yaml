{{- if .Values.ingress.worker.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "pg.worker.name" . }}
  labels:
    {{- include "pg.labels" . | nindent 4 }}
  {{- if .Values.ingress.worker.className }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.ingress.worker.className | quote }}
  {{- end }}
spec:
  {{- if .Values.ingress.worker.tls }}
  tls:
    {{- toYaml .Values.ingress.worker.tls | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.worker.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "pg.worker.name" $ }}
                port:
                  number: {{ $.Values.worker.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
